{
  "name": "uf-product-ui-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "wait-server": "wait-on -t 180000 -i 500 -l http-get://localhost:3000/health",
    "test": "npm run wait-server && npx playwright test --workers=1",
    "test:permission-gates": "npm run wait-server && npx playwright test --workers=1 -g pw-auth-gate-permission",
    "test:hos": "npm run wait-server && npx playwright test --workers=1 -g \"pw-app-bar-mobile-|pw-app-bar-desktop-sticky\"",
    "test:headed": "npm run wait-server && PW_HEADED=1 npx playwright test --workers=1 --headed",
    "test:ui": "npm run wait-server && env -u CI npx playwright test --ui --workers=1"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "wait-on": "^7.2.0"
  }
}
